bin/
*.log
*.sqlite
*.db
unmatched*.csv
/reality-check
/dispatch
/category-test

# vendor data files — contain real vendor contacts + the ERP IDs, keep local only.
# Regenerated from the ERP by the worker's vendor-refresh path.
data/

# macOS + runtime noise
.DS_Store
.claude/

